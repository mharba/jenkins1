pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Build application ...'
            }
        }
        stage('tests') {
            steps {
                echo 'Execution des tests ...'
            }
        }
        stage('deployment') {
            steps {
                echo 'Deploiement application ...'
            }
        }
    }
}